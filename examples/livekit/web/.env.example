# LiveKit Cloud (or self-hosted) — https://cloud.livekit.io
# Must be the SAME project your voice agent  uses, so the browser and the agent meet in one room.

LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
