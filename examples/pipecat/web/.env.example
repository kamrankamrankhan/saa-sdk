# Attention Labs SAA hosted bridge
SAA_API_KEY=

# Daily.co REST API key — https://dashboard.daily.co → Developers tab.
# This server creates ephemeral rooms + mints meeting tokens on your behalf
DAILY_API_KEY=

# Optional — voice-agent provider keys (talkback mode).
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
CARTESIA_API_KEY=
